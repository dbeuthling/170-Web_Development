<!doctype html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>Person</title>
</head>

<body>
<h4>Email:</h4>
  <p><%= @email %></p>
  <h4>Interests:</h4>
  <p><%= @interests.join(", ") %></p>
  <h4>Other users:</h4>
  <p><% @others.each do |name| %>
    <a href="<%= name %>"><%= name %></a>
    <% end %>
  </p>
  <h4>Totals:</h4>
<p>There are <%= @users.size %> <a href="/">users</a>, with <%= count_interests %> interests.</p>
</body>
</html>