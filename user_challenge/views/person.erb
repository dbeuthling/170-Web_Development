<!doctype html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>Person</title>
</head>

<body>
  <p><%= @email %></p>
  <p><%= @interests.join(", ") %></p>
  <p><% @others.each do |name| %>
    <a href="<%= name %>"><%= name %></a>
    <% end %>
  </p>
</body>
</html>